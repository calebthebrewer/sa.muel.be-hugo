box: publysher/hugo
build:
  steps:
    - arjen/hugo-build:
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: sa.muel.be
        basedir: public
        repo: SamuelDebruyn/samueldebruyn.github.io
