box: debian
build:
  steps:
    - arjen/hugo-build
deploy:
  steps:
    - script:
        name: install git
        code: |
          sudo apt-get update && sudo apt-get install git -y
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        basedir: public
